node_modules
*.log
.env
.DS_Store
tools/
*.zip

# runtime data (click counts) - not source
data/

# local test fixtures and helper scripts
_*.html
push.ps1
PUSH-TO-GITHUB.cmd
cookies.txt

# admin-saved remote engine state (boot config)
.engine-state.json
